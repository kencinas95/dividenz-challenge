package com.dividenz.challenge.kencinas;

// When working with SpringBoot, annotate this with @SpringBootApplication
public class DividenzChallengeApplication {

    /**
     * CallCenter application challenge for Dividenz.
     * In this application I made use of the ReactorRX java library in order to manage/handle the concurrency
     * instead of using the default package (java.util.concurrent) because it's so much easier this way.
     *
     * @param args: args
     */
    public static void main(String[] args) {
        // If this app should be a SpringBootApplication, then:
        // SpringApplication.run(DividenzChallengeApplication.class, args);

        // Maybe should I create an API for this?
    }
}
